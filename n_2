#include<iostream>
#include<cmath>
using namespace std;
int f(int n) {
	static int k = 0, sum = 0;
	if (n > 0) {
		sum = sum + n % 10 * pow(2, k);
		k = k + 1;
		f(n / 10);
		return sum;
	}
}
int main() {
	int n;
	cin >> n;
	cout << f(n);
	system("pause");
	return 0;
}
